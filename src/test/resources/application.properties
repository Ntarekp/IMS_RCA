# Test Configuration using H2 In-Memory Database

# Database Configurations
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=PostgreSQL
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver

# JPA/Hibernate Configurations
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.defer-datasource-initialization=true

# Logging
logging.level.root=INFO
logging.level.npk.rca.ims=DEBUG

# JWT Configuration (Test Key)
jwt.secret=ht5TZgosNwAsNRLbRUCurxS9FG5TLyVKRqovve59+dw=
jwt.expiration=86400000

# Email Configuration (Mock)
spring.mail.host=localhost
spring.mail.port=1025
spring.mail.username=test@example.com
spring.mail.password=test
spring.mail.properties.mail.smtp.auth=false
spring.mail.properties.mail.smtp.starttls.enable=false

# App Custom Properties
app.cors.allowed-origins=http://localhost:3000
app.frontend.url=http://localhost:3000
app.file.upload-dir=target/uploads
app.admin.default-email=admin@test.com
